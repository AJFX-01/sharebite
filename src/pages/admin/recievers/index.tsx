import { Grid } from '@mui/material';

const Recievers = () => {
  return (
    <Grid container spacing={{ xs: 2.5, sm: 3 }} mb={3}>
      {/* ------------- Data-Grid section ---------------- */}
      <Grid item xs={12}>
        <Recievers />
      </Grid>
    </Grid>
  );
};

export default Recievers;
