import IconifyIcon from 'components/base/IconifyIcon';

const ApiKeyIcon = () => {
  return <IconifyIcon icon="bitcoin-icons:two-keys-filled" width={25} height={25} />;
};

export default ApiKeyIcon;
