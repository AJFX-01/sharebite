import IconifyIcon from 'components/base/IconifyIcon';

const TeamIcon = () => {
  return (
    <IconifyIcon
      icon="heroicons:user-group-solid"
      sx={{ width: { xs: 20, xl: 25 }, height: { xs: 20, xl: 29 } }}
    />
  );
};

export default TeamIcon;
